# v0.8 B01 product foundation

[Draft PR #554](https://github.com/jihoon22-lee/devbox/pull/554) starts the v0.8
milestone from `6f811df`. B01/#543 and #541/#542 remain open. No merge, release or
stable tag has been produced; product feature migration is still pending.

Four hidden Tauri/React shells consume one catalog and native/React adapters.
Only local main-window describe and bounded route-status commands are exposed.
Requests bind native owner/install/session/context/revision and reject replay.
Public packaging still selects the existing 15 apps. Operation/Problem,
ProjectContext, source-owned AppLink artifact and existing SecretReference
contracts have native/shared fixture consumers; the feature scaffold registers
route, fixture, documentation and validator entries.

The 572-entry baseline inventory records reviewed capability/authority and
registration groups for all 15 apps; every inventoried native command is in its
handler registry. A separate data inventory classifies 37 preservation groups,
including WebView2 localStorage, vault/templates, derived indexes and Runtime
ownership history. None of the pending v0.8 parity entries is marked verified.

Control Center's synthetic migration harness uses WAL-consistent backup,
completed snapshot metadata, a product/install destination namespace and bounded
dry-run mappings/conflicts. Import/validation/cancellation and durable completion
receipts share a DB transaction. Restart, repeat, stale/foreign plans and linked
paths have fixtures. No production importer or renderer path selection is exposed.
See the [foundation contract](../docs/architecture/v0.8-foundation.md) for limits.

Validation:

- Focused contract tests: 8 passed; shared shell tests: 10 passed; TypeScript build
  passed. Migration tests: 6 passed; focused Rust Clippy/fmt passed.
- Prior full `pnpm verify:affected` passed all 19 frontends and the Rust workspace
  in 330.093 seconds (2,121 Rust tests; cgroup peak 3,816,316,928 bytes).
  The final references/migration/installer batch also passed full affected verification
  in 485.997 seconds (2,126 Rust tests; cgroup peak 6,444,388,352 bytes under 8 GiB).
- [CI 34091197385](https://github.com/jihoon22-lee/devbox/actions/runs/34091197385)
  passed all standard gates on PR head `101d875`, including actual Windows API
  cross-entropy rejection and Runtime current-user DPAPI round-trip tests.
- [Windows foundation 34091197376](https://github.com/jihoon22-lee/devbox/actions/runs/34091197376)
  passed four NSIS builds, native WAL/request fixtures and real shell route,
  replay/foreign-install rejection, same-location duplicate exit and two-location
  concurrency. Evidence is the PR merge commit `5886d985`, not a stable candidate.
- New anchor/product installer coexistence uses shared existing NSIS helpers and
  pinned v0.7 tag/manifest/asset digests. Windows PowerShell syntax and installer
  policy tests passed. Run 34093874882 reached installed data readback but failed
  to observe the window-state file. The probe now selects the visible window by
  owned PID and exact title instead of .NET's potentially early/helper handle,
  and retries geometry changes after native setup. Full affected verification
  after this correction passed in 275.289 seconds (cgroup peak 1,929,863,168 bytes).
  [Windows run 34095229758](https://github.com/jihoon22-lee/devbox/actions/runs/34095229758)
  then passed all four real installer pairs on head `8093ba1` (tested PR merge
  `b55a79daf96358ff4745d7c6ff9cd4bcdd0ffddb`). Each pair kept separate ARP/executable,
  shortcuts and data, ran concurrently, and preserved legacy/v0.8 data through
  uninstall. This is development coexistence evidence, not stable promotion.

The opt-in baseline job pins all 32 v0.7 assets and source, and fixes measurement
conditions/budgets before execution. It retains the existing process/data cleanup
contract. Sampler tests reject reused PID cohorts, invalid numbers and unrelated
budget failures. The baseline is separate from release acceptance and R24.

[Baseline run 34099044832](https://github.com/jihoon22-lee/devbox/actions/runs/34099044832)
measured seven cold/input/idle paths, six complete warm paths, 500-file indexing
(627 ms), ten searches (2–3 ms), and terminal profile readback (3 ms). The host
could not enumerate WSL distributions; this is unavailable, not zero distros or
live PTY restoration. All 15 process/data cleanup checks passed. Seven pinned
source bundle builds and budget checks passed, but JSON publication failed on
the checker's BigInt metrics; output now preserves exact decimal strings.

The same immutable v0.7 Runtime binary failed `run_job_now` with
`run-execution-failed` in both baseline runs. Its platform adapter passes relative
`cmd.exe` as non-null `lpApplicationName`, for which Windows does not search PATH
([CreateProcessW contract](https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/nf-processthreadsapi-createprocessw)).
The correction resolves `GetSystemDirectoryW` plus `cmd.exe` in the app's Windows
layer. It preserves command source, working directory, suspended creation,
explicit handle inheritance, Job Object assignment and cleanup. A native test
runs the owned shell from a synthetic task directory containing a cmd.exe decoy,
checks stdout/exit status and confirms the process tree is gone. Actual Windows execution of this test passed in CI 34101371613; WSL
check/Clippy/fmt also pass.

The pinned baseline characterizes only that exact source/command/error plus a
single durable `spawn-failed` run. It records `result=failed`, `budget.passed=false`
and `r24Passed=false`, while retaining warm/idle and cleanup evidence. Unknown
failures, another source commit or any additional budget violation still fail
the recording job. The corrected current code has no such acceptance exception.
WP05 owns the integrated Runtime execution/latency proof; WP06 owns live PTY.

[CI 34101371613](https://github.com/jihoon22-lee/devbox/actions/runs/34101371613)
passed every standard check on implementation head `55e5586`, including the new
owned-system-shell execution regression and the two DPAPI owner tests.
[Windows foundation 34101371619](https://github.com/jihoon22-lee/devbox/actions/runs/34101371619)
passed all four native/installer proofs, baseline characterization and seven
bundle records (tested PR merge `d37e9341e2599088011eaf321b600bf6b0269792`).
The 15 original runtime/data-cleanup checks passed; six measured budgets passed,
and Runtime's exact legacy spawn failure remained a failed budget. The durable
[measurement record](../docs/architecture/v0.8-baseline-measurements.json) retains
source/host/conditions and every measured value without user data or local paths.

The earlier Markdown Table action-test race is fixed by explicitly flushing its
deferred formatter before action assertions; all six focused tests passed.
Final affected verification of the implementation took 286.065 seconds (2,126
portable Rust tests, cgroup peak 2,189,266,944 bytes). This final documentation
update changes no implementation; final PR check status is available in
[PR #554](https://github.com/jihoon22-lee/devbox/pull/554).

B01's foundation artifacts, native authority and WAL/resume boundaries, Windows
identity decision and baseline characterization have been reviewed. Production
feature/importer parity, live PTY restoration, integrated performance and suite
activation remain B02–B09 work. Neither B01 nor the known failed legacy baseline
completes R24, #541/#542 or stable promotion.
