# v0.8 B01 product foundation

The open v0.8 milestone requires complete consolidation and stable publication.
This work begins B01 in `feat/workspace/v08-foundation`, based on `6f811df`.
The implementation remains a draft; #541/#542/#543 and the milestone stay open.

Four hidden Tauri/React shells consume one validated product catalog and shared
native/React adapters. The native surface is limited to local main-window
description and bounded route-status requests with session/replay validation.
Development copies use distinct runtime namespaces. The legacy public selector
still contains 15 applications; no release or tag is created by this bundle.

The synthetic Control Center migration harness uses SQLite online backup with
WAL-aware snapshots and a transactional import mapping journal. It has no
production migration command. A 572-entry source inventory tracks pending owner
review without claiming existing features have been migrated.

Semantic changes: new development topology/shells, native request validation,
migration harness, cross-language affected edges, and hidden-app verification.
Pure moves: none. Generated metadata: issue-contract snapshot and source inventory.
Data/authority decisions and remaining gates:
[foundation contract](../docs/architecture/v0.8-foundation.md).

Validation performed:

- Four product frontend builds and per-product axe smoke tests passed.
- Shared shell tests: 4 passed (wire fixture, bounded navigation, keyboard/IME/a11y).
- Catalog tests: 13 passed; product request/context tests: 4 passed.
- Migration tests: 3 passed (committed WAL/uncommitted writer, source preservation,
  corrupt/future/cancelled snapshots, tamper detection, rollback/repeat/conflict).
- Catalog/release-contract script passed; accessibility covers 19 apps; bundle
  and accessibility checker regression tests passed; affected graph tests passed.
- `pnpm verify:affected` selected full scope and passed in 772.654 seconds.
  This single run also satisfies the verifier-change full audit: 19-app builds,
  frontend tests/typechecks, Rust check/Clippy/fmt/workspace tests. Memory limit
  remained 8 GiB, with cgroup peak 6,444,564,480 bytes. The earlier run found a
  Manager fixed-15 catalog assertion; it was corrected to distinguish 15 public
  apps from four hidden targets, then the complete run passed.
- GitHub CI, Windows packaged shell/installer execution and performance baseline:
  not yet run. No Windows PASS or feature-parity completion is claimed.

Next: finish required verification, open the B01 draft review packet and complete
the outstanding #543 gates before dependent bundles or stable cutover.

The native project-context follow-up adds opaque project/worktree/target IDs and
revision binding, with shared Rust/TS fixtures and stale/foreign-context rejection.
Windows CI run 34085896573 found an existing Workbench test that compared two
independently generated wall-clock timestamps across a second boundary. Its
read-back test now validates the real timestamp interval and compares every other
envelope field without requiring the separate projections to share a second.
Follow-up `pnpm verify:affected` passed full scope in 344.594 seconds (cgroup
peak 4,175,015,936 bytes). Windows foundation run 34085896646 built all four
NSIS packages and passed the native request/WAL fixtures, but the GUI probe
could not locate the executables: a relative Cargo target was resolved separately
under each pnpm app cwd. The workflow now uses the absolute repository target
root, with a regression check for build/probe agreement. Final Windows CI and
GUI execution must be rerun; neither failed run is recorded as overall PASS.

The second Windows foundation run reached the copied executables but could not
open CDP through environment flags. The existing release smoke harness already
handles elevated WebView2 with an owned per-image HKLM policy. Its helpers are
now safely importable without starting acceptance or altering signal handlers,
and the foundation fixture reuses them with distinct image names and restoration
in every cleanup path. This follows [Microsoft's elevated-host override rules](https://learn.microsoft.com/en-us/microsoft-edge/webview2/concepts/security#for-an-elevated-host-app-use-appropriate-override-flags).
The import regression, existing release smoke self-test and complete local
`pnpm verify:affected` passed (283.024 seconds, cgroup peak 1,950,584,832 bytes).
CI run 34087332445 passed all standard gates, including Windows workspace tests,
for the preceding cbeadca head. Final CI/GUI evidence must match the new helper
commit; the prior failed GUI run is not reused as PASS.
