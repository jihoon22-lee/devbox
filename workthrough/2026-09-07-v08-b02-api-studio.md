# B02 API Studio consolidation

Tracking: #544, master #541, acceptance #542; draft PR #555. Implementation
in progress; full product parity and migration acceptance remain open.

## Changes

- Extracted API Playground's six pure protocol modules into `crates/api-protocols`.
  Legacy API commands and the new API Studio API component share the same engine.
- Moved the three frontend implementations and their 623 tests into
  `packages/api-studio-features`. Legacy entry points consume the same exports.
  API Studio lazily opens Requests/Protocols, Webhooks and Transforms; mounted
  feature groups retain their drafts and native connections across navigation.
- Added explicitly enumerated native adapters around the existing commands.
  New product startup initializes state without running legacy bootstrap or
  starting requests/listeners/processes. Storage roots use the product namespace
  and component subdirectories. Internal API→Transforms and Webhook→API sends
  use product-owned one-time storage; no legacy executable is launched. Other
  cross-product sends remain unexposed until their receiving owners are integrated.
- Legacy crates default to their `standalone` executable feature; API Studio
  disables it and links only the native component implementation. This keeps
  legacy frontend bundles and bootstraps out of the product build. The original
  standalone apps keep their existing default behavior.
- Product IPC checks the native main window, registered component owner, session,
  installation, context, replay, admission deadline and bounded arguments before
  dispatch. Active IDs stay reserved across long awaits. Responses carry native
  operation provenance; dialogs and explicit clipboard reads use that boundary.
- Catalog revision 3 declares API Studio's three component authorities and its
  separate startup importer authority.
  Public v0.7 selection remains 15 apps. Historical source paths are retained
  alongside relocated inventory paths.

- Internal sends return `ArtifactReference` with native source provenance. Webhook
  sends load source-owned masked history/fixtures by opaque ID; API selections
  reuse the existing redactor. Pending deliveries cannot overwrite unread actions.
  Busy-receiver failures revoke publication. API/Transforms retain claim, preview,
  renewal, explicit apply and restore using the same product-owned store. Scoped
  wake-ups open the recipient view, whose data comes from native pending state.
- Restored standalone entry-point CSS and real shared-feature axe smoke tests for
  the three legacy apps. This preserves their accessibility gate after the move.

## Verification so far

- `cargo test -p api-protocols`: 51 PASS after pure extraction.
- `cargo check -p api-playground -p webhook-lab -p developer-toolbox`: PASS.
- `cargo check -p devbox-api-studio`: PASS with component dispatch and native pickers.
- Shared frontend typecheck and API Studio production build: PASS. Build emits
  separate Protocol Lab, OpenAPI import, Webhook and transform chunks.
- Shared frontend: all 623 tests PASS. The no-argument legacy IPC regression
  found during extraction was corrected; focused MCP/gRPC rerun: 17 PASS.
- Product UI: 2 PASS, including an actual request draft across protocol/webhook
  navigation. Product native/session/catalog focused tests: 23 PASS.
- `pnpm verify:affected` selected all: PASS (757.58 seconds). All 19 frontend
  bundle budgets, builds, tests, TypeScript, workspace Rust check/Clippy/fmt and
  2128 portable Rust tests passed. Resource supervisor enforced the normal
  budget; measured cgroup memory peak 6,444,843,008 bytes.
- Locked dependency policy/notices check: PASS. Windows probe syntax/workflow
  regression: PASS; actual Windows runtime checks await this PR CI.
- Disposable Chromium browser fixture: Requests, Webhooks, Transforms and
  Protocols rendered at 1440×1000 with no horizontal overflow. This is not a
  Windows/native runtime result.
- `pnpm verify:all` after CI fixes: PASS (536.109 seconds), including all
  frontend checks and 2128 portable Rust tests; cgroup memory peak
  6,445,223,936 bytes. Component-only API Studio Clippy also passes with warnings
  denied. `cargo check -p devbox-api-studio --features tauri/custom-protocol`
  passes with all three legacy `dist` directories temporarily absent. The fixture
  restored those generated directories afterward; no native app ran on Linux.
- The first Windows CI exposed legacy asset embedding and two conditional unused
  imports. CI also exposed a UI test assuming lazy transformation finished within
  one second and a packaged-marker checker limited to app-local source paths.
  The first fixes passed locally; the next CI exposed a second lazy-test ordering
  issue (imports were awaited before async description mounted the feature) and
  missing standalone accessibility entry points. Both are now corrected. The marker checker follows declared production workspace
  dependencies with cycle/unrelated/dev-only regression coverage. Catalog checks
  pass. OAuth also initializes its required native system-browser plugin.
- Internal handoff batch: 286 focused Rust tests PASS across API, Webhook,
  Transforms and product, including namespace isolation, one-time consumption and
  publication rollback on a busy receiver. Shared shell/product UI 12 tests PASS;
  all three standalone axe tests and 19-app accessibility contract PASS. Shared UI
  and product builds PASS. Windows fixture now exercises redacted preview,
  explicit UI apply and rejected second consumption; execution remains pending.
- Windows [foundation run 34111082483](https://github.com/jihoon22-lee/devbox/actions/runs/34111082483):
  PASS for PR head `20248cf` (tested merge `eb440c7`). Four product installers
  build, coexist with v0.7 anchors, preserve legacy markers and uninstall their
  own registrations. Two isolated API Studio copies open the actual Requests UI,
  leave the listener stopped, execute native SHA256 and reject replay, wrong
  component and foreign installation. Windows CI check/Clippy/test also PASS.
  This run predates the internal handoff batch. Full R24/S03 acceptance remains open.
- Final internal handoff batch `pnpm verify:affected` selected all: PASS
  (532.331 seconds), all frontend checks and 2132 portable Rust tests.
  Resource limits remained enforced; cgroup memory peak 6,444,609,536 bytes.
  Both unpublished product and standalone native builds passed strict Clippy.
- Product metadata validator: PASS; parity entries remain pending until acceptance.

## Remaining acceptance

API Workspace IDs/project linkage, distinct History/Console view, typed internal
capture/request/transform/mock/Knowledge handoffs, persistent-source migration and
resume/conflict fixtures, listener close policy, final native/error-lifetime review,
S03 local fixture server test, final affected verification and final Windows CI evidence.
Do not close #544 before these checks pass. B03–B09 remain separate bundles.

## Internal handoff Windows follow-up

The `05fbfc3` Windows run built the products but timed out waiting for the internal
handoff preview. Catalog and frontend CI now pass. A regression fixture using the
actual hosted frontend transport passes locally. Navigation now remains in native
pending state until a mounted consumer peeks and acknowledges its exact ID; event
payloads only wake the reader. This also covers delayed listener registration.
The Windows probe retains bounded synthetic UI diagnostics on failure. The failed
run is not counted as internal handoff acceptance; the next Windows run must verify
the complete preview/apply path.

Validation of this follow-up: `pnpm verify:affected` selected all and PASS in
735.628 seconds (2,132 portable Rust tests, all frontend checks including the new
hosted-transport UI regression; peak cgroup memory 6,445,043,712 bytes). Windows
preview/apply acceptance remains pending the follow-up head.

The follow-up Windows run for `5850eb6` passed general CI but still timed out in
CDP during the native flow. The probe now records the last completed stage and
bounded synthetic renderer exceptions, logs and dialog events, so the next run
can distinguish command delivery, renderer failure and an unexpected dialog.
No native assertion or timeout budget was relaxed.

Diagnostics-only follow-up validation: `pnpm verify:affected` selected all and
PASS in 292.827 seconds, including 2,132 portable Rust tests and all frontend
checks. Peak cgroup memory was 4,955,676,672 bytes. Probe syntax and workflow
contract checks pass; native handoff acceptance remains open.

The instrumented `1804ef1` run localized the timeout to `describe`, before any
handoff publication. Native authorization held the session mutex while calling
`WebviewWindow::url()` from an async command. That getter can dispatch to the UI
thread, whose synchronous `describe` command also waits for the session mutex.
Caller label and local origin are now collected before locking the session.
Authority checks are preserved; the Windows native run must confirm the fix.

Session-lock fix validation: `pnpm verify:affected` selected all and PASS in
315.743 seconds, including 2,132 portable Rust tests and frontend checks. Peak
cgroup memory was 4,143,538,176 bytes. Windows native confirmation is pending.


## Legacy import implementation

The Webhook/Transforms pure engines and Control Center snapshot/transaction
harness now have real API Studio consumers in `crates/webhook-core`,
`crates/transforms-core` and `crates/data-migration`. Mechanical moves are kept
separate from importer wiring. The affected graph checks all reverse consumers;
the capability checker enforces the main/isolated-export window separation.

The startup importer reads actual API browser keys and sealed OAuth/TLS files,
Webhook fixtures/service profiles and Toolbox workflow metadata. Closed LevelDB
acquisition holds read-only exclusive source handles and validates file/list
identities. A suspended child assigned to the existing owned process Job exports
only from a copied WebView2 directory, verified through COM before reading keys.
No legacy bootstrap runs on the original. Native DPAPI validation reuses the API
owner; unavailable secrets require reconnect. Raw copies and tickets are purged;
new previews supersede old staging while pending activation remains protected.

Domain merge preserves ordered duplicate headers, excludes transient/raw/HMAC
state, resolves supported ID/name conflicts, enforces capacity and records source
fingerprints. Store-qualified plan IDs avoid collisions across stores. Completed
receipts preserve later product edits on repeat import. SQLite accepts the intent;
CAS native writes, browser read-back and final acknowledgment activate it. This
is not a cross-store transaction: startup is gated until completion, restart can
resume or restore only recorded values, and unexpected edits block overwrite.

Focused portable validation: 141 tests PASS across the product importer, three
shared cores and journal, including interrupted activation, rollback/reimport,
source-ID overlap across stores, cancelled/tampered plans and staged-copy cleanup.
Product/shared frontend builds PASS. The pinned actual v0.7 executable fixture
checks original hashes, real DPAPI reuse, unavailable secrets, repeat imports,
product edits and absence of automatic traffic/listeners on Windows; it has been
written but has not run for this importer batch. Final affected validation selected all and PASS in 710.486 seconds: 2,147
portable Rust tests, all frontend checks (628 shared feature tests, 6 product
tests), all 19 bundle budgets, Rust check/Clippy/fmt. Peak cgroup memory was
6,444,769,280 bytes under the normal resource limit. Catalog, graph and workflow
contract checks also PASS. Windows acceptance of this importer remains pending.

The session-lock fix `1160151` passed the Windows foundation workflow
[34125444959](https://github.com/jihoon22-lee/devbox/actions/runs/34125444959)
(tested merge `2a8dc338ba836b54b49e3e9a93420f28c1c96d18`). Native evidence confirms
both isolated API Studio installations complete API→Transforms redacted preview,
explicit apply and one-time consumption, with owner/replay/foreign-installation
rejections and no listener auto-start. All four NSIS installation coexistence,
legacy marker/data separation and uninstall checks pass. This closes the observed
native deadlock regression; it does not cover the uncommitted importer batch.

The first importer Windows run (`121229c`, run 34127426694) built all products
and seeded the pinned legacy app successfully, but failed at the first preview
control. Startup markup displays that button before asynchronous source discovery
has enabled it. The fixture now waits for the actual enabled control before
clicking, retaining its existing timeout and source-preservation assertions.
The migration itself remains unverified until this follow-up Windows run.

Preview-control readiness fix: `pnpm verify:affected` selected all and PASS in
706.141 seconds (2,147 portable Rust tests, all frontend checks and 19 bundle
budgets); cgroup memory peak 6,444,773,376 bytes. Probe syntax PASS. Windows
fixture rerun remains required.

The enabled-control follow-up reached the importer and correctly rejected the
Windows fixture's `text → json-format` pipeline, which violates the preserved
Toolbox type transition. The native synthetic stores now live in one fixture
consumed by both Rust owner validation and Windows acceptance; its pipeline uses
JSON input. The regression also proves the invalid transition stays rejected.
Bounded native stage names distinguish source validation, copied export and plan
merge failures without returning source contents or raw errors.


## History, errors and listener ownership

History/Console now separates persisted HTTP requests, gRPC summaries and bounded
current connection status. Selecting a record or collection in API Studio opens
a preview; only an explicit replacement changes the existing request draft.
Protocol sessions remain mounted and their message/timeline payloads stay with
the protocol owner. The new route is lazy and uses existing safe projections.

A compiled, component-specific error contract preserves fixed legacy cancellation,
stale-connection, OAuth reconnect and handoff expiry failures. Native and renderer
validate the same allowlist after provenance checks. Interpolated/unknown backend
errors remain excluded. Startup source-discovery failures can be skipped only if
the native owner permits startup; profile selection respects the existing 64-item
batch bound without silently widening domain limits.

The temporary Webhook listener defaults to stop-and-quit on window close. Explicit
keep-listening requires an available native tray, supports hide/reopen/stop, and
full quit stops the process-owned listener. Existing interruptible stop/socket
cleanup is reused. Service exports remain disabled/non-autostart definitions;
`--service-profile <id>` starts only a validated listener in an independent process
without UI, product IPC or API session bootstrap. UI and Runtime-owned workers
share profile identity but cannot stop each other's in-memory listener.

Focused validation: product UI 10 PASS; affected legacy UI regressions 77 PASS;
product native tests 22 PASS plus existing Webhook listener regressions 4 PASS;
strict Clippy PASS. Production build/initial budget PASS (234,376 raw bytes,
72,994 gzip; unchanged 280,000/90,000 limits). Disposable browser screenshots at
1440 and 900 pixels verify the selected History preview without horizontal overflow;
Webhook close controls also rendered. This is not Windows native execution.
The new Windows lifecycle fixture tests close/hide/reopen/full-quit, inactive
export, headless service execution, port collisions and independent process/socket
cleanup. It will run independently after a successful product build even if the
separate importer fixture fails; failures still fail the job. Final `pnpm verify:affected` selected all and PASS in 698.690 seconds: 2,151
portable Rust tests, 628 shared feature tests, 10 product UI tests, all 19 bundle
budgets and build/typecheck/check/Clippy/fmt gates. Peak cgroup memory was
6,444,830,720 bytes under the normal resource limit. Catalog, dependency and
workflow checks PASS. Actual Windows validation of this batch remains pending.

## Transform output policy and durable Knowledge fallback

The fixed `apps/api-studio-tools.json` manifest matches all 21 actual tool
components and exposes route-only global command identities for WP07. Native
publication checks its policy and the full typed pipeline transition chain.
Unknown sources and HMAC are rejected; HMAC's existing explicit copy/file save
remain available. Generic HMAC/pipeline handoff buttons no longer bypass the
legacy restrictions. Eligible product tool/pipeline output is masked natively,
published to the installation's one-time Requests receiver and explicitly applied.
It neither launches a legacy executable nor sends a request automatically.

Both Requests and Transforms now own bounded, masked Knowledge draft files.
Explicit save is distinct from delivery: the unavailable receiver is accurately
reported and History/Console provides later reopen/export/delete. Durable typed
artifact IDs are metadata, not authority or expiring AppLink leases. Native
owner/installation paths, OS locks, atomic writes, bounded reads, schema validation
and explicit deletion preserve restart/full-store/corrupt-version boundaries.
WP07 must negotiate a real Knowledge receiver before any delivered state is used.

The `1b2f8de` Windows run [34132948706](https://github.com/jihoon22-lee/devbox/actions/runs/34132948706)
built all products and passed native tests/baseline but failed two stale fixture
assumptions: consumed handoffs now return a classified failed operation, and the
debug console was counted as a service window. Fixtures check the failed outcome
and hide debug consoles; the worker also asserts that it creates no WebView.
Migration now runs independently after successful builds even if another fixture
fails. The general CI failure was the real lazy Requests module exceeding the
DOM helper's one-second wait; the focused test keeps the actual route and awaits
readiness within a bounded ten seconds. These are pending Windows rerun, not PASS.

Focused checks: 71 Rust tests PASS (product 26, transform core 37, contract 8),
53 affected feature UI tests PASS across the focused run and corrected newline
fixture rerun, product UI 10 PASS, typecheck/build/strict Clippy PASS. Both Windows
scripts and 13 embedded CDP expressions compile. The Knowledge confirmation was
visually checked at 1440/900 pixels in the explicitly labelled browser preview;
it has no horizontal overflow and focuses Cancel. No native save is claimed from
that browser check. Final `pnpm verify:affected` selected all and PASS in 344.808
seconds: 2,157 portable Rust tests, 631 shared feature tests, 10 product UI tests,
all 19 bundle budgets and all build/typecheck/check/Clippy/fmt gates. Cgroup peak
was 5,122,871,296 bytes under the normal limit. Catalog/capability/graph/workflow
checks PASS. Initial JS is 234,717 raw / 73,077 gzip bytes within unchanged limits.
Actual Windows acceptance of this batch remains pending.

## Mock drafts, comparison inputs and Windows snapshot correction

Responses and eligible tool/pipeline outputs now publish a typed, one-time Mock
preview in the installation's handoff store. OpenAPI reuses the existing Mock
parser's supported operation/path/status projection. Its blank response body is
an editable draft, not an invented response example. The recipient rebuilds and
validates a rule with a fresh editor identity and fixed content type only. HTTP
matching targets are distinct from filesystem paths; no response credentials or
execution flags cross this boundary. Busy previews are retained, tokens stay
native, and explicit apply/cancel consumes once. Apply changes only the editor;
existing rule-conflict confirmation, actual rule storage and listener startup are
separate. Accepted response selections can explicitly populate either comparison
input, and product tool navigation retains both inputs in memory.

`86b74cf` general [CI 34136085694](https://github.com/jihoon22-lee/devbox/actions/runs/34136085694)
passed. [Windows 34136085675](https://github.com/jihoon22-lee/devbox/actions/runs/34136085675)
passed both installation namespaces, one-time internal delivery, native HMAC
rejection and durable masked Knowledge fallback/owner rejection. The actual
importer failed at `api-snapshot`: its identity helper reopened an exclusively
held source file with `GENERIC_READ`. Identity now opens attributes only, retaining
all source-data guards and content-capable handles for actual reads. This matches
the [Windows sharing contract](https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-createfilew).
A Windows-only fixture exercises the actual exclusive handles and unchanged source
bytes; its execution remains pending. Service-window acceptance still failed after
hiding the debug spawn console. Explicit service mode now detaches its own console
and the fixture records window class/visibility for diagnosis; no pass is claimed.

Focused checks: product/Filesystem portable tests 47 PASS, relevant shared UI
87 PASS across the focused group and new OpenAPI case, native transport/comparison
UI regression PASS, typecheck and strict Clippy PASS. Windows fixtures now exercise
Mock preview/apply with no active rule/listener mutation and retain screenshots.
Actual Windows importer/lifecycle/Mock acceptance remains pending.

The first full run stopped on the new recipient's initial-focus timing. Focus now
runs before paint and only when the Webhooks route is visible, including a preview
queued while that route was hidden. The focused visibility/apply/cancel regression
passes. Final `pnpm verify:affected` selected all and PASS in 596.498 seconds:
2,159 portable Rust tests, 636 shared feature tests, 10 product UI tests, all 19
bundle budgets and build/typecheck/check/Clippy/fmt gates. Cgroup peak was
6,444,736,512 bytes under the normal limit. Catalog/capability/graph/workflow
checks PASS. Initial JS is 235,042 raw / 73,138 gzip bytes within unchanged limits.


## API Workspace and saved OpenAPI operations

API-owned Workspace metadata links actual Collections/environments, new saved
OpenAPI operation projections and persisted Webhook service profiles. Selection
only filters lists: request drafts, selected environment and network/protocol
lifetimes remain independent. Native CAS revisions, UUID identities, OS locks,
atomic writes, bounds and closed schemas reject stale writes and preserve corrupt
or future files. Standalone workspaces are usable now; Project association derives
only from the already-authorized request context, never a renderer path. The real
Project provider remains WP07 work. Deleting a Workspace removes its ID set only.

OpenAPI saves supported operation projections, not an invented legacy store or
an archive of unsupported source semantics. Native persistence reuses the actual
request wire codec, request secret inventory and DPAPI environment sanitizer.
A regression caught an unmasked header when only the generic JSON sanitizer was
used; request-owned inventory now includes disabled headers and copied bearer
tokens while preserving exact references and duplicate header order/enabled flags.
Records reopen as previews and require explicit Requests replacement or Mock
publication. Native deletion is reflected even when a subsequent list refresh
fails, with an accurate incomplete-refresh message. Legacy executable labels are
preserved; product-hosted feature headers use the product's group names.

`43c2782` [general CI 34140013469](https://github.com/jihoon22-lee/devbox/actions/runs/34140013469)
PASS. Its [Windows 34140013476](https://github.com/jihoon22-lee/devbox/actions/runs/34140013476)
passed product builds/native tests, the new exclusive source-handle regression,
and both installation namespaces' one-time masked Mock preview/editor-only apply.
The importer now gets beyond snapshot but fails in its worker export stage. Bounded
native worker phase records were added; raw copies and their phase files are purged
with the existing cleanup. Windows lifecycle diagnosed the remaining handle as
Tao's zero-size layered/non-activating thread event target, not a WebView. Its
fixture now checks that exact class, zero area and all four extended-style flags;
all real interactive windows still fail. Source: pinned Tao 0.35.3
`platform_impl/windows/event_loop.rs::create_event_target_window`.
The separate pinned legacy baseline failed Launcher descendant identity/cleanup;
its evidence is retained, and this is not treated as a product acceptance pass.

Focused product Rust tests 33 PASS, Workspace/OpenAPI UI 17 PASS, product UI 10
PASS, TypeScript and strict Clippy PASS. The Windows fixture now checks Workspace
stale-write/project-unavailable rejection, saved-definition native masking/reopen
and explicit selection preserving an unsent request. Both scripts and 31 embedded
CDP expressions compile. A synthetic, clearly labelled browser preview of the
actual components was checked at 1440/900 pixels with 240 pixels reserved for the
sidebar: editor and saved-operation preview have no horizontal overflow. The
preview performs no native work; temporary preview files/processes were removed.
Actual Windows Workspace/importer/lifecycle acceptance remains pending. S03, full parity mapping and subsequent B03–B09/release work remain open.


The initial affected run was deliberately stopped (SIGTERM, exit 143 after
196.372 seconds) after baseline investigation found an unsafe PID-reuse edge in
the packaged verification harness. Its descendant walk checked ParentProcessId
without creation ordering, so an older unrelated process could be counted as a
child of a newly reused PID. The failed artifact's 139 Launcher descendants
motivated inspection; it did not retain the identities needed to prove that exact
failure's lineage. A pure fixture reproduces the faulty ordering independently.
The corrected walk requires current root identity and nondecreasing creation times
on every parent edge. Inventory, action revalidation and performance sampling now
share invariant UTC timestamps with all seven fractional digits. Post-exit candidates
remain uncertain and never grant kill authority; incomplete executable identity
still blocks termination. Recycled roots, sub-millisecond older children, backward
grandchildren and post-exit uncertainty are covered by the required self-test.
This follows the [Win32_Process ParentProcessId contract](https://learn.microsoft.com/en-us/windows/win32/cimwin32prov/win32-process).
Final `pnpm verify:affected` selected all and PASS in 330.916 seconds: 2,164
portable Rust tests, 644 shared feature tests, 10 product UI tests, all 19 bundle
budgets and build/typecheck/check/Clippy/fmt gates. Cgroup peak was 5,323,567,104
bytes under the normal limit. Initial JS is 235,250 raw / 73,193 gzip bytes within
unchanged limits. This full run also satisfies the verifier-change full audit;
no resource limit or acceptance threshold was relaxed.


## S03 acceptance, headless service runner and parity mapping

The actual S03 fixture now connects a native capture to a request preview, supplies
an empty synthetic secret reference, answers the real UI prompt to seal the
credential with DPAPI, and approves exactly one request to an owned loopback server.
It checks credential receipt, masked native response, comparison input/differences,
explicit Mock editor-only apply and Knowledge's accurate unavailable-but-preserved
fallback. Restart must retain the masked draft and reject another owner. No raw
credential is seeded in storage, no request/listener starts automatically, and
native execution is never inferred from browser mocks. The Windows job will also
run the API/Webhook/Toolbox crates' actual domain tests instead of only compiling
them as product dependencies. S03 is registered independently after successful
product builds so another fixture failure does not hide its result.

`7d4979d` [general CI 34143372108](https://github.com/jihoon22-lee/devbox/actions/runs/34143372108)
and the separate legacy baseline job in [Windows 34143372092](https://github.com/jihoon22-lee/devbox/actions/runs/34143372092)
PASS, including the corrected UTC process identity checks. The product fixture
failed with a startup CDP evaluation timeout, without renderer exception/crash
records; startup attachment now selects a real app document and can reattach only
within the same readiness deadline. No authority invocation is retried.
Migration worker phases proved that export reached result creation; the failure
is after that point. Native stage classification now distinguishes process exit,
Job cleanup, result read and owned-copy removal, without raw error/path exposure.
Migration is still not accepted.

The actual Tao event target was 16×16 despite zero creation dimensions. Service
mode now avoids constructing a Tauri App/event loop altogether. An `OwnedProfile`
uses the existing listener/rules/framing/connection bounds and interruptible stop
implementation directly; its process/Job owns the lifetime. The interactive app's
stop semantics are unchanged. Profile lookup uses the same native executable-derived
installation identifier and desktop local-data root. The Windows fixture again
requires no window handle, with no internal-window exception. A portable native
fixture verifies listener availability, duplicate-port rejection and drop cleanup.

All 137 B02 legacy anchors and 10 data groups now name implementation, actual test
files and source/destination policies. Their statuses remain pending until actual
semantic acceptance; the checker verifies mapped file references/counts. The two
Webhook→Logs entries now expose the existing source-owned bounded projection to
WP07 and return a specific unavailable result while retaining their source. They
never fall back to legacy executable launch or raw vault access. Workspace Logs
and Knowledge delivery are explicitly handed to #549; B02 provides their producer
contracts and the required safe fallback.

Focused checks: API Studio 33 plus Webhook 5 native tests PASS; 55 Webhook UI tests
PASS; strict Clippy PASS after moving the new adapter above the Rust test module.
Catalog, workflow contract and 55 cooked CDP expressions compile/PASS. The S03,
headless process, newly expanded Windows domain tests and post-export diagnosis
remain pending actual Windows execution. Final `pnpm verify:affected` selected
all and PASS in 318.066 seconds: 2,165 portable Rust tests, 644 shared feature
tests, 10 product UI tests, all 19 bundle budgets and build/typecheck/check/Clippy/fmt
gates. Cgroup peak was 4,144,001,024 bytes under the normal limit. Initial JS is
235,350 raw / 73,225 gzip bytes within unchanged limits.

### Windows acceptance corrections and Requests loading

On `a7b6a23`, [Windows 34146766214](https://github.com/jihoon22-lee/devbox/actions/runs/34146766214)
passed the expanded API/Webhook/Toolbox domain tests, four-product native probes,
both installation namespaces, Workspace/OpenAPI persistence and redaction, and
the legacy performance baseline. The service had no window handle. Migration
completed worker/result/Job cleanup but failed specifically removing its owned
export copy; migration acceptance remains open. Copy removal now retries for at
most two seconds, rechecking root identity and links each time, and reports fixed
failure categories without exposing paths. It never changes source permissions.
Two portable retry/replacement tests pass; a real Windows delete-lock fixture is
added but awaits execution.

The lifecycle fixture now reads the actual main webview visibility because
`Process.MainWindowHandle` can describe Tao's hidden event window. The S03 fixture
explicitly selects its seeded environment before reconnecting its empty secret
reference, matching the application's existing environment-selection behavior.
Both corrected Windows scenarios still require execution. General CI's sole
failure was a stale Cargo.lock digest in notices; the generator updates that digest.

Separating parser-independent OpenAPI limits removes YAML from the default Requests
closure. Same-build, same-Node-gzip measurements decreased from 565,320 raw / 173,370
gzip to 432,851 / 131,909 bytes. This measures static assets, not Windows startup
latency. A build gate rejects eager OpenAPI/Protocol/Transforms entry points or the
YAML engine, and retains both the existing API ceiling and outer-shell budget.
Its positive/negative self-tests and workflow/metadata checks pass. Focused OpenAPI
package tests (2), shared importer/limits tests (28), strict API Studio Clippy and
the product build pass. All 65 embedded CDP expressions in the four Windows
fixtures compile. Dependency notices now match both lockfiles.

Final `pnpm verify:affected` selected all and passed in 469.759 seconds: 2,167
portable Rust tests, 644 shared feature tests, 10 product UI tests and all 19
bundle budgets, with build/typecheck/check/Clippy/fmt gates. The default Requests
closure is 432,851 raw / 131,909 gzip bytes; the outer shell is 235,394 / 73,251.
Cgroup memory peaked at 6,444,470,272 bytes under the normal 8 GiB maximum.
Windows execution of this revision remains pending.

### Product runtime measurements

The API lifecycle fixture now records cold renderer readiness and an inert F24
acknowledgement, waits for ten-second startup survival, then samples the UI process
and verified descendants for five idle seconds before starting any listener. It
reuses the baseline inventory, UTC process identity and sampler. Hidden-window
relaunch timing is measured in the later lifecycle scenario with an explicit UI
listener and independent service. Reports identify that different warm workload
and the hidden debug build; these are not packaged-release speed comparisons.
The existing cold/input/warm/CPU/working-set/process-count budgets remain fixed.
Sampler negative tests, packaged-smoke self-tests and script syntax checks pass;
actual runtime numbers and final affected verification remain pending.

### Replacement comparison and scratch cleanup boundaries

`a6de660` [general CI 34150321191](https://github.com/jihoon22-lee/devbox/actions/runs/34150321191)
passes all jobs. [Windows 34150321232](https://github.com/jihoon22-lee/devbox/actions/runs/34150321232)
passes domain/native/installation checks, the baseline and the complete listener
lifecycle: default close, hidden listening, reopen, full quit, independent service,
port collisions and socket release. S03 reaches capture, native DPAPI reconnect,
one explicit send and a masked response, then fails at comparison rendering.
The real native `DiffOp::Replace` adapter emitted only a delete hunk, so the new
column disappeared. It now emits delete plus insert while preserving the wire
format. The regression reconstructs changed new lines between unchanged context.
All four focused native diff tests pass.

Migration still fails in cleanup with `api-export-copy-path-rejected`; that broad
category does not identify the exact failing filesystem operation. Root identity,
root/child path and entry metadata failures now have separate fixed stages. A
bounded, metadata-only Windows fixture report inspects this run's newly created
staging directory, without reading files or following links.

Owned scratch cleanup treats child symlinks/reparse entries as leaves and uses
the standard library's [non-following removal contract](https://doc.rust-lang.org/std/fs/fn.remove_dir_all.html).
Root and ancestor links remain forbidden, root identity is revalidated before
each retry, and source acquisition still rejects links. Tests prove that a child
link's outside target survives, a linked root is rejected, a replaced root is
preserved, and transient removal retries do not touch the source. These four
portable checks and strict API Studio/Toolbox Clippy pass; their actual Windows
execution and the complete importer/S03 scenarios remain pending.

Final affected all passes in 316.082 seconds: 2,170 portable Rust tests, 644 shared
UI tests, 10 product UI tests, all 19 bundle budgets and build/typecheck/check/
Clippy/fmt gates. Cgroup peak is 5,070,422,016 bytes under the unchanged 8 GiB
maximum. The Requests closure and outer-shell asset measurements are unchanged.

### S03 and runtime acceptance; disappearing scratch entries

On `e986c36`, [Windows 34152328677](https://github.com/jihoon22-lee/devbox/actions/runs/34152328677)
passes the complete S03 flow, including native DPAPI reconnect, exactly one send,
replacement comparison, Mock editor-only apply, Knowledge fallback, restart and
owner isolation. The full listener lifecycle and its fixed runtime budgets also
pass: cold ready 2,288 ms, input acknowledgement 6 ms, warm hidden-window restore
114 ms, idle working-set sum 367,407,104 bytes / 8 processes / sampled 0% CPU.
The reported sample interval is 5,732 ms and its process cohort remains unchanged.
These are debug-build observations with the previously stated warm workload.

Migration now identifies an entry-metadata failure. By the later fixture inspection,
the raw copy is gone and only two staging directories and one file remain, with no
links or metadata errors. This is consistent with cleanup racing a pending deletion,
rather than proof of a persistent linked path. Scratch inspection now accepts an
enumerated child that is already absent and retries transient metadata/directory
reads within the existing deadline. Root identity, link and entry-count checks
remain in force; failure to inspect the root is not treated as absence. Six focused
cleanup tests pass, including deterministic disappearing-entry and transient-access
regressions; strict Clippy and 66 cooked CDP expressions pass.

The separate Control Center foundation startup probe timed out without a renderer
exception, after the other three products passed. Startup failure now records the
bounded document state to distinguish a loading/IPC error from wrong attachment.
Its acceptance condition and deadline are unchanged. The importer and final
four-product Windows run remain open.

Final affected all passes in 477.149 seconds: 2,172 portable Rust tests, 644 shared
feature UI tests, 10 product UI tests, all 19 bundle budgets and build/typecheck/
check/Clippy/fmt gates. The normal 8 GiB limit remains in force; cgroup peak is
6,444,691,456 bytes. Requests and outer-shell asset measurements are unchanged.
