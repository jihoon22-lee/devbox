# B03 Knowledge, Activity and Search

Refs #545, #541 and #542. One B03 PR (#556) owns frontend extraction,
native product integration, source-aware search, import/activation, fixtures and
this record. B01/B02 are merged; B04/B06/B07 own the external providers below.

## Behavior and review boundaries

The legacy Notes, Life Log and Everything+ UI trees are shared through
`packages/knowledge-features`; legacy entries keep the same feature implementations.
Knowledge lazily loads Notes/Daily, Activity and Search, retains visited drafts,
and shares civil-date selection without creating notes automatically. Notes'
static closure excludes Activity, file extractors and Mermaid, below the original
970,000 raw / 325,000 gzip ceiling. Feature CSS is scoped; the shared shell's
controls style only their intended shell elements.

Typed native adapters reuse the three original engines while fixing private
product storage and common snapshots per process. Native authorization separates
Notes writer, Activity collector, Search query/index settings, opener and migration.
Collection starts OFF; imported consent does not silently enable collection.
Close-to-tray is an explicit persisted setting; normal close otherwise exits.

Daily, Activity and session summaries use existing claim/preview/cancel/exclusive
save/ack contracts. Summary metadata is bounded and allowlisted; commands, logs,
window titles and secret values are not automatically copied. Git aggregation
preserves common repository/worktree provenance and source/range diagnostics.

Search preserves separate Notes/file FTS, full filters/settings/extractors and
nested-root ownership. Source queries return bounded generations and explicit
partial/stale/unavailable states. A 500 ms regex worker deadline permits recovery.
File/current-project work shares bounded workers and retained-handle retirement;
SQL applies the verified project prefix before LIMIT. Opening uses opaque refs
and rechecks generation, owner, source, row revision and pinned file/root/project
objects. Renderer paths, replaced objects and cancelled references are rejected.

The native Project Registry projection invalidates stale project queries and
preserves unmapped/offline/ambiguous Activity rows. Its schema-v1 fixture carries
exact B01 context and native path binding. B04/B07 own actual authenticated
publication/distro resolution; B06 owns real session-summary publication and retry
with the same descriptor. Producer/receiver fixtures are not claims that those
transports are connected.

## Data and recovery

The explicit importer snapshots WAL consistently, validates pinned v0.7 schemas
and writes three unselected destination databases. Authoritative templates,
settings, history, roots/exclusions/saved queries and reserved deleted-root IDs
are mapped; derived FTS/link indexes are rebuilt and transient deliveries are
retired. Search meta accepts only known schema/ownership/allocator keys and exact
`pdf-v1`, `docx-v1`, `xls-v1`, `xlsx-v1`, `ods-v1` markers from v0.7.
Unknown/future/corrupt input is rejected without replacing it with an empty store.

Activation requires reviewed plans, source writer quiescence and unchanged
source/destination snapshots. Repeat imports do not duplicate data or replace
new product edits. Recovery preserves both generations and refuses rollback over
newer writes. Markdown/assets remain in the user vault; executable rollback does
not undo external vault edits. Native owner leases exclude competing product
installations and active legacy writers, including spelling aliases.

A scheduled vault change keeps the live binding until restart. On next start,
preview/cancel/apply checks folder identity and ownership, keeps existing notes
and templates, and selects a new source identity. Incomplete/offline scans preserve
last-good data; filesystem work stays off startup/query admission.

The [requirement and table/file map](../docs/architecture/v0.8-knowledge.md) links
101 legacy features and seven preserved groups to implementations/tests. Older
Activity Timeline input and browser-owned settings need explicit B08 report paths;
they are not silently represented as migrated.

## Actual verification

Head `b4f417371616962fe45cb030da338b843399f70a` passed focused importer tests
(6) and strict Clippy in 31.723 seconds, then final affected all in **530.993
seconds** with the shared 8 GiB cap (cgroup peak 6,444,945,408 bytes).
[General CI 34196340573](https://github.com/jihoon22-lee/devbox/actions/runs/34196340573)
passed. Its first Windows build attempt encountered `os error 32` while copying
Tauri resources; only the failed job was rerun at the same commit and passed.

[Windows acceptance 34196340663](https://github.com/jihoon22-lee/devbox/actions/runs/34196340663)
passed **every stage**. The private `knowledge-migration.json` records source
`94cd22bc111b9cbb64d505286e504415270c8dc6` (the PR merge commit for that head).
Actual pinned v0.7 executables produced the source profiles. Verified behavior:

- WAL import/activation, original schema/files, templates/settings/queries/history/
  privacy and deleted-root reservation; old writer and second-install owner denial.
- Notes name/body FTS and opaque in-product opening; replaced-object, raw-path and
  cancelled-reference rejection; regex deadline and recovery.
- Activity preview/cancel/explicit save/single consumption; repeat import,
  preservation of new edits and rollback refusal over them.
- Vault preview/cancel/rebind, source-identity change and original files/templates.
- Tray hide, secondary invocation restoring the same owner, persisted close
  preference after forced crash, normal default exit, and owned profile cleanup.
- Native authority/WAL, API Studio workflows/lifecycle and installer coexistence.

Configured Windows **debug** measurements passed the unchanged budget: cold
renderer 983 ms, first input observed 992 ms (F24 acknowledgement 6 ms), warm
existing-window restoration 327 ms. Idle CPU was 0% across 7 owned processes;
summed working sets were 365,760,512 bytes (shared pages may be counted twice).
500-file indexing took 486 ms; ten exact source queries took 90–102 ms including
polling and identity checks. The host/conditions are in the artifact. This does
not complete the packaged release comparison or the remaining suite R24 workloads.

## Remaining acceptance

An added hosted-runner fixture registers a dedicated, digest-pinned Ubuntu WSL1
rootfs, exercises actual UNC aliases/case-sensitive notes, approved vault rebinding,
atomic writes, 500-file body search/polling, replaced objects, outside symlinks,
unavailable-root last-good preservation and polling reconnect, then removes only
its verified per-run distro. Node syntax checks pass; **actual execution is pending**.
An owned folder rename represents root unavailability, not WSL2 VM suspension.
Windows interop resumed after the session interruption and confirmed the host's
running Ubuntu is WSL2. Three ignored, explicit Windows unit-test fixtures now
exercise Notes atomic/case-sensitive IO and unavailable/reconnect behavior,
500-file content indexing and last-good reconciliation, and opaque source object
identity/cancellation. They require a UUID-marked owned UNC folder and never start
an app or open a legacy/product profile. The fixture-only manual job exports only
those Windows test executables plus source/digest metadata for one day. The local
runner validates each digest and requires one real passing test per executable.
These new native tests have not yet executed; WSL2-specific acceptance is pending.

The new WSL fixture/workflow changes need affected verification and final CI/native
execution before merge. B04/B06/B07 connections and B09 packaged performance remain
owned by their respective bundles. #545 is not complete and the PR remains draft.

The first added-fixture affected run exposed a shared-target build-script cache
from the other worktree (Knowledge permission registration was absent from the
cached script, while present in source). Source mtimes were refreshed without
content changes. That retry was interrupted when the session/WSL restarted
(exit 143), not counted as a pass. The resumed affected all passed in **700.911
seconds** (8 GiB enforced; cgroup peak 6,446,137,344 bytes). Subsequent explicit
Windows WSL fixtures/private export changes require the final run below. Node
syntax, workflow YAML and actual Windows PowerShell parser checks passed.

Final focused checks passed in **50.209 seconds**: source-search 5 and vault 6
portable tests, Node fixture syntax and product metadata. Windows-only owned-WSL
tests remain intentionally unexecuted locally until their Windows artifacts are
built; PowerShell parsing ran on the actual Windows host.

The final added-fixture state passed affected all in **318.220 seconds** with the
shared 8 GiB budget (cgroup peak 6,444,769,280 bytes). This covers the final source,
workflow and fixture changes locally. Windows compilation/execution of the new
ignored tests and hosted WSL1 product exercise remain pending on this commit.


Windows fixture-only run 34202083818 built all three test executables from
`56d301161153d8b41d1e7cb50822daf4801d8a7f`. On the actual Windows 11 / Ubuntu
WSL2 host, Notes atomic/case/alias/unavailable/reconnect passed (one test, 0.30 s),
and product opaque source identity/cancellation passed (one test, 0.04 s).
Search indexed all 500 filenames but the fixture incorrectly expected its content
API to return 500 instead of the preserved 200-row ceiling. That assertion failed
before unavailable/reconnect and is not a product defect or a full WSL2 PASS.

The fixture now separately verifies all 500 FTS rows and the 200-result API cap.
The hosted WSL1 fixture likewise uses five disjoint 100-file content groups to
prove full corpus coverage. Both corrections require new native execution.
The local PowerShell runner used a process-only ExecutionPolicy override for the
reviewed repository script; no persistent host policy was changed.


The hosted WSL1 run 34201991019 prepared and cleaned its dedicated Ubuntu distro
and passed approved WSL vault binding/case-distinct atomic edits. Its next
assertion incorrectly required all 500 remote result objects to finish verification
inside the existing 1.5 s source-query deadline, then reported a generic indexing
failure. The fixture now records index/query state, requires all 500 cached rows,
and separately checks exact native references across the corpus. Large remote
queries retain their bounded partial-result contract. The pending WSL2 source
fixture also checks the forward-slash UNC spelling used by the index projection.

The corrected bounded-query fixture state passed focused Node syntax,
source-search (5 portable tests) and last-good reconnect regression (1 portable
test) in **40.482 seconds**. The new Windows-only forward-slash and content-cap
assertions remain pending native compilation/execution on the next commit.

The final bounded-query and Windows-only fixture corrections passed affected all
in **645.870 seconds** (cgroup peak 6,445,072,384 bytes, 8 GiB cap). All frontend
and Rust gates passed. New Windows/WSL1 and local native WSL2 execution remains
required for the changed assertions; portable PASS does not replace those runs.
