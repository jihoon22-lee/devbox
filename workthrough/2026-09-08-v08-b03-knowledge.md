# B03 Knowledge, Activity and Search

Refs #545, #541 and #542. One B03 PR (#556) owns frontend extraction,
native product integration, source-aware search, import/activation, fixtures and
this record. B01/B02 are merged; B04/B06/B07 own the external providers below.

## Behavior and review boundaries

The legacy Notes, Life Log and Everything+ UI trees are shared through
`packages/knowledge-features`; legacy entries keep the same feature implementations.
Knowledge lazily loads Notes/Daily, Activity and Search, retains visited drafts,
and shares civil-date selection without creating notes automatically. Notes'
static closure excludes Activity, file extractors and Mermaid, below the original
970,000 raw / 325,000 gzip ceiling. Feature CSS is scoped; the shared shell's
controls style only their intended shell elements.

Typed native adapters reuse the three original engines while fixing private
product storage and common snapshots per process. Native authorization separates
Notes writer, Activity collector, Search query/index settings, opener and migration.
Collection starts OFF; imported consent does not silently enable collection.
Close-to-tray is an explicit persisted setting; normal close otherwise exits.

Daily, Activity and session summaries use existing claim/preview/cancel/exclusive
save/ack contracts. Summary metadata is bounded and allowlisted; commands, logs,
window titles and secret values are not automatically copied. Git aggregation
preserves common repository/worktree provenance and source/range diagnostics.

Search preserves separate Notes/file FTS, full filters/settings/extractors and
nested-root ownership. Source queries return bounded generations and explicit
partial/stale/unavailable states. A 500 ms regex worker deadline permits recovery.
File/current-project work shares bounded workers and retained-handle retirement;
SQL applies the verified project prefix before LIMIT. Opening uses opaque refs
and rechecks generation, owner, source, row revision and pinned file/root/project
objects. Renderer paths, replaced objects and cancelled references are rejected.

The native Project Registry projection invalidates stale project queries and
preserves unmapped/offline/ambiguous Activity rows. Its schema-v1 fixture carries
exact B01 context and native path binding. B04/B07 own actual authenticated
publication/distro resolution; B06 owns real session-summary publication and retry
with the same descriptor. Producer/receiver fixtures are not claims that those
transports are connected.

## Data and recovery

The explicit importer snapshots WAL consistently, validates pinned v0.7 schemas
and writes three unselected destination databases. Authoritative templates,
settings, history, roots/exclusions/saved queries and reserved deleted-root IDs
are mapped; derived FTS/link indexes are rebuilt and transient deliveries are
retired. Search meta accepts only known schema/ownership/allocator keys and exact
`pdf-v1`, `docx-v1`, `xls-v1`, `xlsx-v1`, `ods-v1` markers from v0.7.
Unknown/future/corrupt input is rejected without replacing it with an empty store.

Activation requires reviewed plans, source writer quiescence and unchanged
source/destination snapshots. Repeat imports do not duplicate data or replace
new product edits. Recovery preserves both generations and refuses rollback over
newer writes. Markdown/assets remain in the user vault; executable rollback does
not undo external vault edits. Native owner leases exclude competing product
installations and active legacy writers, including spelling aliases.

A scheduled vault change keeps the live binding until restart. On next start,
preview/cancel/apply checks folder identity and ownership, keeps existing notes
and templates, and selects a new source identity. Incomplete/offline scans preserve
last-good data; filesystem work stays off startup/query admission.

The [requirement and table/file map](../docs/architecture/v0.8-knowledge.md) links
101 legacy features and seven preserved groups to implementations/tests. Older
Activity Timeline input and browser-owned settings need explicit B08 report paths;
they are not silently represented as migrated.

## Actual verification

The preserved shared UI/native suites, importer tests and strict Clippy passed.
The earlier fixture state `fb7d37e8036a5fbcb27d2d8be8697d5003922995`
passed affected all in **645.870 seconds** under the shared 8 GiB cap (cgroup
peak 6,445,072,384 bytes) and [general CI](https://github.com/jihoon22-lee/devbox/actions/runs/34206082579).

[Windows acceptance 34196340663](https://github.com/jihoon22-lee/devbox/actions/runs/34196340663)
passed every stage for head `b4f417371616962fe45cb030da338b843399f70a` / tested
merge `94cd22bc111b9cbb64d505286e504415270c8dc6`. Actual pinned v0.7 executables
produced the source profiles. The later [fb7d37e Windows run](https://github.com/jihoon22-lee/devbox/actions/runs/34206082626)
again passed the following Knowledge behavior before its WSL unavailable-root step:

- WAL import/activation, original schema/files, templates/settings/queries/history/
  privacy and deleted-root reservation; old writer and second-install owner denial.
- Notes name/body FTS and opaque in-product opening; replaced-object, raw-path and
  cancelled-reference rejection; regex deadline and recovery.
- Activity preview/cancel/explicit save/single consumption; repeat import,
  preservation of new edits and rollback refusal over them.
- Vault preview/cancel/rebind, source-identity change and original files/templates.
- Tray hide, secondary invocation restoring the same owner, persisted close
  preference after forced crash, normal default exit, and owned profile cleanup.

The later report identifies tested merge `d5aae70ab0902d5130b179046971b5fcf5dbf370`.
Configured Windows debug budgets passed: cold 1,094 ms, first input 1,097 ms,
warm existing-window restore 415 ms, idle 0% across 7 owned processes with
368,115,712 bytes summed working sets, 500-file indexing 800 ms and ten exact
searches 91–105 ms. Shared memory may be counted twice. These are configured
debug measurements, not the remaining packaged suite R24 comparison.

Actual **Windows→Ubuntu WSL2** tests from exact
`951e8260e52d49c996b415b09c5e31b47d47e1c1` passed all three
[private test executables](https://github.com/jihoon22-lee/devbox/actions/runs/34217294216):
Notes case/alias/CRLF atomic write and unavailable/reconnect (0.33 s), Search
500-file filename/FTS indexing, preserved 200-result content cap, last-good and
reconnect convergence (8.67 s), and opaque object replacement/cancellation plus
forward-slash UNC (0.06 s). The exact source, SHA256 and byte size of each binary
were verified before execution on Windows 10.0.26200. JSON evidence was retained
and only the two identity/marker-checked fixture directories were removed. No
product bootstrap, legacy database, user vault or distro shutdown was involved.

The latest [951e826 Windows run](https://github.com/jihoon22-lee/devbox/actions/runs/34217255451)
(tested merge `5470cea3d08d54941a831bc1646bd9613382c626`) passed native
owner/WAL/Webhook checks, all API Studio flows, shell lifecycle and installer
coexistence. Knowledge passed import/recovery/vault/privacy and its debug budget.
Hosted **WSL1** passed approved vault binding/case edits, 500-file filename/body
coverage with the existing 200-row content cap, exact usable references, alias
identity and replaced-object/outside-symlink denial. Cancellation drained to
**zero workers and zero native objects**, and the Linux corpus move succeeded.
The unavailable-root query then returned **0 rather than 500 cached matches**,
so the augmented WSL acceptance remains failed; its exact query state was not
recorded by that fixture version.

The index now keeps last-good rows visible throughout replacement scans and
prunes unseen paths only after all batches and final native root revalidation.
A connection-local, bounded scan table tracks paths without changing persistent
schema or clearing templates/settings. Root handles/identity span traversal and
extraction. Queued incremental events validate the root and parent before
interpreting a missing child; access errors preserve rows and request a full
reconcile. Deterministic regressions cover cancellation after traversal, root
movement before publication and queued events while the root is offline, then
successful reconnect/deletion. The Windows fixture records offline query state,
resource bounds, index counts and cached watcher health before assertions.
The 1.5-second source query deadline is unchanged. The **172 native Rust tests**
(137 Search and 35 Knowledge), strict Clippy and fixture syntax passed in
**20.328 seconds**. Final affected all passed in **649.623 seconds** under
the shared 8 GiB cap (cgroup peak 6,445,379,584 bytes). Native reconciliation
re-execution remains pending.

The same run passed actual API Studio migration, listener/service lifecycle,
end-to-end workflow and installer coexistence after accepted sockets explicitly
returned to blocking mode. It exposed a separate native regression: Windows could
leave a pending synchronous read blocked after a cloned socket's shutdown, and the
partial-body stop test took about five seconds instead of less than one.

The connection adapter now polls blocking reads/writes at 100 ms while preserving
the five-second overall request and response IO budgets. Cancellation is checked
between waits; response timing starts with the first write, so configured rule
delays retain their behavior. HTTP framing/parsing now accepts `Read`/`Write`
without cloning the connection. Existing allocation, worker, history and framing
bounds remain. The regression spans multiple poll intervals for delayed first
bytes/split bodies and verifies cancellation without relying on socket shutdown.
All **82 core and 7 native tests**, strict Clippy and WSL fixture syntax passed in
**10.240 seconds**. The [7535d01 Windows run](https://github.com/jihoon22-lee/devbox/actions/runs/34212645451)
then passed all native authority tests, API Studio flows, lifecycle and installer
checks; only the Knowledge WSL corpus move failed.
[Winsock accept contract](https://learn.microsoft.com/en-us/windows/win32/api/winsock2/nf-winsock2-accept).

The latest configured Knowledge debug budget passed (cold 1,170 ms, first input
1,201 ms, warm 549 ms, idle 0.07%, summed working sets 370,388,992 bytes, index
991 ms, ten queries 91–108 ms). Shared pages may be counted twice; R24 packaged
suite acceptance is still separate.

## Remaining acceptance

The prior 865456a state passed final affected all in **615.183 seconds** (cgroup
peak 6,445,301,760 bytes, 8 GiB cap). The current cancellation adapter and native
Linux corpus move passed final affected all in **687.965 seconds** (cgroup peak
6,445,453,312 bytes, same 8 GiB cap) and
[general CI including Windows](https://github.com/jihoon22-lee/devbox/actions/runs/34212645383).
The source resource reporting and WSL fixture passed final affected all in
**810.243 seconds** under the same 8 GiB cap (cgroup peak 6,445,002,752 bytes).
The 951e826 general CI passed, while its augmented Windows WSL acceptance failed
at cached result publication as described above. Native re-execution of the
reconciliation fix remains required. Then update the feature/data mapping with exact executed evidence and final CI.
B04/B07 own authenticated Project Registry/provider delivery, B06 owns actual
session publication, and B08/B09 own suite activation and packaged release
acceptance. Those boundaries do not grant cross-owner authority through a route.
