[package]
name = "catalog"
version = "0.1.0"
publish = false
description = "Validated devbox app catalog parsing and capability selection"
authors = ["jihoon22.lee"]
edition = "2021"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
