[package]
name = "secrets"
version = "0.1.0"
description = "Secret sealing trait, masking and versioned blob envelope (pure)"
edition = "2021"

[lib]
name = "secrets"
path = "src/lib.rs"

[dependencies]
zeroize = { version = "1", features = ["zeroize_derive"] }
