[package]
name = "secrets"
version = "0.1.0"
publish = false
description = "Secret sealing, shared DPAPI, masking and versioned blob envelope"
edition = "2021"

[lib]
name = "secrets"
path = "src/lib.rs"

[dependencies]
ts-rs = { workspace = true }
serde = { workspace = true }
zeroize = { workspace = true }

[target.'cfg(windows)'.dependencies]
windows = { workspace = true }
