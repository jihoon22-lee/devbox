# transforms-core

Cross-platform domain logic extracted from developer-toolbox for reuse by its legacy native
entry point and API Studio. Native window, session, process and clipboard authority
remain in the app adapters. Existing domain tests move with their implementation.

Run `cargo test -p transforms-core` from the repository root.
