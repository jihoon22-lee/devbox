[package]
name = "integration"
version = "0.1.0"
publish = false
description = "devbox integration snapshot envelope, path and atomic write"
edition = "2021"

[lib]
name = "integration"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
