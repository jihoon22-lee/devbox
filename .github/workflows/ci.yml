name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend:
    name: Frontend (pnpm)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build frontends
        run: pnpm build

      - name: Type check
        if: ${{ hashFiles('apps/*/tsconfig.json', 'packages/*/tsconfig.json') != '' }}
        run: pnpm -r exec tsc --noEmit

  rust:
    name: Rust (Cargo workspace)
    runs-on: ubuntu-latest
    # 앱/크레이트가 하나도 없으면(계획 단계) 이 잡은 실행하지 않는다.
    if: ${{ hashFiles('apps/*/src-tauri/Cargo.toml', 'crates/**/Cargo.toml') != '' }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Install Linux system libs
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev libgtk-3-dev build-essential \
            libssl-dev libxdo-dev libayatana-appindicator3-dev librsvg2-dev

      - name: Check
        run: cargo check --workspace

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Format
        run: cargo fmt --all --check

      - name: Test
        run: cargo test --workspace
